import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://disenopaginas.cl',
  base: '/productos-muestra',
  // output: 'static' es el default — genera HTML puro listo para subir a cualquier hosting
});
